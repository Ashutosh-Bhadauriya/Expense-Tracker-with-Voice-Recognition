import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  income: {
    borderBottom: "10px solid rgba(240, 224, 0, 1)",
  },
  expense: {
    borderBottom: "10px solid rgba(240, 224, 0, 1)",
  },
}));
